<%= form_with(model: minimu, local: true) do |form| %>
  <% if minimu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minimu.errors.count, "error") %> prohibited this minimu from being saved:</h2>

      <ul>
      <% minimu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :short_url %>
    <%= form.text_field :short_url %>
  </div>

  <div class="field">
    <%= form.label :long_url %>
    <%= form.text_field :long_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
